/**
 * Re-export the @Public() decorator from the auth guard module.
 * This provides a convenient import path for feature modules.
 */
export { Public, IS_PUBLIC_KEY } from '../../../common/guards/auth.guard';
